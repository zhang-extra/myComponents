"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.whatInlineTypes = exports.whatBlockTypes = exports.whatMarkTypes = void 0;

var _immutable = require("immutable");

var whatMarkTypes = function whatMarkTypes(_ref) {
  var value = _ref.value;

  if (value.marks) {
    return value.marks.map(function (mark) {
      return mark.type;
    });
  }

  return new _immutable.List();
};

exports.whatMarkTypes = whatMarkTypes;

var whatBlockTypes = function whatBlockTypes(_ref2) {
  var value = _ref2.value;

  if (value.blocks) {
    return value.blocks.map(function (block) {
      return block.type;
    });
  }

  return new _immutable.List();
};

exports.whatBlockTypes = whatBlockTypes;

var whatInlineTypes = function whatInlineTypes(_ref3) {
  var value = _ref3.value;

  if (value.inlines) {
    return value.inlines.map(function (block) {
      return block.type;
    });
  }

  return new _immutable.List();
};

exports.whatInlineTypes = whatInlineTypes;