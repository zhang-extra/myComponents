'use strict';

var _require = require('../../'),
    Serializer = _require.Serializer,
    MARKS = _require.MARKS;

/**
 * Serialize a sub text to HTML
 * @type {Serializer}
 */


var serialize = Serializer().transformMarkedRange(MARKS.SUB, function (state, text, mark) {
    return '<sub>' + text + '</sub>';
});

module.exports = { serialize: serialize };