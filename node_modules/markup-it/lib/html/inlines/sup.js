'use strict';

var _require = require('../../'),
    Serializer = _require.Serializer,
    MARKS = _require.MARKS;

/**
 * Serialize a sup text to HTML
 * @type {Serializer}
 */


var serialize = Serializer().transformMarkedRange(MARKS.SUP, function (state, text, mark) {
    return '<sup>' + text + '</sup>';
});

module.exports = { serialize: serialize };